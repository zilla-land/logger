/** Representation of the various log-level operators. */
export enum LogLevelOperator {

    /** An equal (==) operator. */
    EQUAL = 0,

    /** A greater-than-or-equal (>=) operator. */
    GREATER_OR_EQUAL = 1,

    /** A less-than-or-equal (<=) operator. */
    LESS_OR_EQUAL = 2

}
